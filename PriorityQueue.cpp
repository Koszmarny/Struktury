//
// Created by pawel on 30.05.2019.
//

#include "PriorityQueue.h"
#include "Queue.h"

PriorityQueue();

PriorityQueue(double[]

*_elements)

void PriorityQueue::attach(int value) {
    Queue.attach(value);
}

void PriorityQueue::detach(int value) {
    Queue.detach(value)
}

int PriorityQueue::front() {
    return this.Queue.front();
}

bool PriorityQueue::increaseKey(int value, int newPriority) {

}

bool PriorityQueue::isEmpty() {
    return this.Queue.isEmpty();
}